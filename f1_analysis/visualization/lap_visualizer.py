def plot_speed_vs_lap_progress() -> None:
    """Speed vs lap progress plot for each driver in one figure."""


def plot_throttle_heatmap() -> None:
    """Heatmap plot of throttle over time.

    x-axis is the time in percent, y-axis is the drivers, and the color indicate the
    speed.
    """


def plot_brake_heatmap() -> None:
    """Heatmap plot of brake over time.

    x-axis is the time in percent, y-axis is the drivers, and the color indicate the
    brake.
    """


def plot_gear_trace() -> None:
    """Gear trace plot."""
